sortArrayPhotographer => 
need getRequest ( need myInit, myRequest, page)
retour une array sans doublons des photographes 

updateParams => 
need patch, createPage1 ( qui need  createName,
  createCity,
  createTagline,
  createPrice,
  createTags,
  createPortrait,
  createLink,
  buttonPage1,
  createButtons,), et need sortArrayPhotographer

  readme comment lancer le projet, le projet est disponible sur port blabla. 

  