creer une factory, une factory de quoi? 

Deja crée = > 
1) une classe photographer avec une fonction createCard() qui crée un custom element quand elle est appelé
2) un custom element 

3) Fonction (createPhotographers) crée qui :
    1) await le json photographer
    2) crée pour chaque élement du tableau un new photographer
    3) je dois appeler create photographer , elle ne se déclenche pas directement
    2eme pb : je dois appeler la fonction createCard sur chaque photographer. 

A creer ? 
Quand clique sur un lien d'un photographer = > 
1) envoi vers photographer-page.html/ ( id ou nom du photographer)
avec cet ID (ou nom) => doit récupérer les infos du photographer et appeler un nouveau custom element ( photographer-header) et renvoyer  les bonnes images ( dans un custom elements)

Creer une factory page ? 
1) Si url est index.html et que hash = '' alors je dois j'appelle createPhotographer et je crée les cartes. 


on each button => 
window.location.hash = "button.id"


Est ce que je dois creer les méthodes de tri sur le custom elem directement? Ou est ce qu'au clic sur le lien a, cela déclenche une fonction qui sera appelé pour load la page qui convient? 

const createElement = (vnode) => {
  if (typeof vnode === 'string') {
    return document.createTextNode(vnode);
  }

  const el = document.createElement(vnode.tag);

  if (vnode.props) {
    Object.entries(vnode.props).forEach(([name, value]) => {
      el.setAttribute(name, value);
    })
  }

  if (vnode.children) {
    vnode.children.forEach((child) => {
      el.appendChild(createElement(child));
    })
  }

  return el;
};
const el = (tag, props, children) => ({
  tag,
  props,
  children,
});
https://developer.mozilla.org/en-US/docs/Web/API/Element/replaceWith
